# CORE website

@PROJECT_CONTEXT.md

@AGENTS.md

Both files above are imported automatically. `PROJECT_CONTEXT.md` holds the rules that
are enforced by the build; `AGENTS.md` holds the repo map, conventions and doc index.
Keep rules in `PROJECT_CONTEXT.md` — do not restate them here, or the two copies will
drift.
